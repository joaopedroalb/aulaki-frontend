import styled from "styled-components";

export const TagBG = styled.div`
    padding: 12px;
    background-color: var(--l-blue);
    color: var(--black);
    border: none;
    border-radius: 40px;
    font-weight: 700;
`